# build

Generated Checkout Blocks payment script for OneKhusa RTP (`npm run build`).

- `checkout-blocks.js` / `checkout-blocks.asset.php` — enqueued on block Cart/Checkout when the gateway is enabled.

For local development, edit `src/checkout-blocks/index.js` then run `npm install` and `npm run build`.

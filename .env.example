# Copy to ".env" and adjust. Docker Compose loads ".env" for variable substitution.
# See docs/ngrok-tunnel.md

# Public HTTPS base for WordPress (ngrok / tunnel). No trailing slash.
# WORDPRESS_PUBLIC_URL=https://your-subdomain.ngrok-free.app
